01. Enable migrations in Package Manager Console

enable-migrations

02. Create firt migration

For the first migration we have to use -IgnoreChanges

add-migration InitialModel -IgnoreChanges -Force


03. Downgrading Database (Go back to specific migration)

update-database	-TargetMigration:Name_Of_Migration